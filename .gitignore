# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
.swiftpm/

# XcodeGen-generated project — project.yml is the source of truth.
# Run `xcodegen generate` after cloning to produce this.
Shipped.xcodeproj/

# Real secrets never get committed — see Secrets.swift.example for the template.
Shared/Secrets.swift
